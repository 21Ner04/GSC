"use client";

/**
 * Accessibility widget placeholder.
 * Re-enable when you have the real UserWay Account ID from
 * https://manage.userway.org → Installation.
 *
 * Then set NEXT_PUBLIC_USERWAY_ACCOUNT_ID in .env.local and restore the loader.
 */
export default function UserWayWidget() {
  return null;
}
